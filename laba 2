'''
Лабораторная работа №2
Написать программу, которая, читая символы из бесконечной последовательности (эмулируется конечным файлом, читающимся поблочно), распознает, преобразует и выводит на экран лексемы по определенному правилу. Лексемы разделены пробелами. Преобразование делать по возможности через словарь. Для упрощения под выводом числа прописью подразумевается последовательный вывод всех цифр числа. Регулярные выражения использовать нельзя.
Вариант 17.
Нечетные двоичные числа, не превышающие 409610, в которых встречается ровно одна серия из трех подряд идущих нуля. Выводит на экран цифры числа, исключая нули. Отдельно выводится прописью номер позиции, с которой начинается эта серия.
'''

import re

with open("C://Users//User//Desktop//политех//ура.txt", 'r', encoding="utf8") as file:
    line = file.readline()
    file.seek(0)                                                                           # Вернуть указатель файла к началу

    for i in file:                                                                         # открываем файл
        index = 0
        line = i.rstrip()                                                                  # читаем строку
        if len(re.findall('000', line)) == 1:
            print("Число:", i)
            for i in re.finditer('000', line):
                index = i.start()
                output = re.sub('000', '', line)
                if len(output) != 0:
                    print("Число, исключая '000':", output)
                else:
                    print("Убрали все символы", end='\n')
                print("Номер позиции:", index, end='\n')
                print()
        else:
            continue
    print()
